twist_mux:
  ros__parameters:
    topics:
      navigation:
        topic: /nav2/cmd_vel
        timeout: 0.5
        priority: 10

      teleop:
        topic: /teleop/cmd_vel
        timeout: 0.5
        priority: 20

    locks:
      emergency_stop:
        topic: /emergency_stop_lock
        timeout: 0.0
        priority: 255